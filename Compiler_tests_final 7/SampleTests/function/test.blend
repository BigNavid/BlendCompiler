function (int) fib(int n)
{
    if(n = 0 || n = 1) {
        return n;
    }

    int res = fib(n-1) + fib(n-2);

    return res;
}

function (int) main()
{
    write(fib(10));
    int rett := 0; return(rett);
}
