-- break

function (int) main()
{
    int i := 0;
    while(i < 10) {
        write(i);
        i := i+1;

        if(i = 3) {
            break;
        };
    };

    int rett := 0; return(rett);
}
